[build]
builder = "RAILPACK"

[railpack]
aptPkgs = [
    "libxcb1",
    "libxrender1",
    "libxext6",
    "libsm6",
    "libglib2.0-0"
]

[deploy]
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port 8000"

[variables]
QT_QPA_PLATFORM = "offscreen"
